@_AUTOGENERATED_FILE_WARNING@
#pragma once

struct @_TARGET_NAME@VersionInfo {
  const char* ProjectName;
  const char* ProjectVersion;
  const char* Major;
  const char* Minor;
  const char* Patch;
  int IsDirty;
  const char* GitCommitHash;
  const char* GitUserName;
  const char* GitUserEmail;
  const char* GitCommitDate;
  const char* Compiler;
  const char* CompilerVersion;
  const char* Processor;
  const char* Configuration;
};

//! fills a struct with the project version information
void @_TARGET_NAME@_init(struct @_TARGET_NAME@VersionInfo* vinfo);

//! returns a string containing all the version info (designed for printing)
const char* @_TARGET_NAME@_summary_string();
